name: iOS network hunt
description: Sysdiagnose / iOS network IoCs with external destinations
query: 'platform="ios" parser="network_iocs" (dest_ip=* OR destination_domain=* OR ioc_value=*) | fields timestamp, source, bundle_id, dest_ip, destination_domain, ioc_kind, ioc_value | sort -timestamp | head 100'
